# --- Client configuration (copy to .env for local overrides) ---

# Base URL of the API, including the /api suffix.
# Local dev default (used if unset): http://localhost:4000/api
# Production example: https://your-api.onrender.com/api
VITE_API_URL=
